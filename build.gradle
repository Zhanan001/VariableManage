plugins {
    id 'java'
}

group = 'com.zhanan'
version = '1.0.0'
sourceCompatibility = '11'
targetCompatibility = '11'

repositories {
    mavenCentral()
    // Nukkit / NukkitX artifacts are hosted on opencollab and sometimes on jitpack
    // Use the public opencollab maven repository that hosts Nukkit artifacts
    maven { url 'https://repo.opencollab.dev/repository/maven-public/' }
    maven { url 'https://jitpack.io' }
}

dependencies {
    // Use Nukkit (NukkitX) as a compileOnly dependency so CI can resolve the API from Maven
    // Replace the version if you need a specific release. If resolution fails, try a different coordinate or add the snapshots repo.
    compileOnly 'cn.nukkit:nukkit:1.0-SNAPSHOT'
}

jar {
    manifest {
        attributes(
            'Implementation-Title': 'VariableManage',
            'Implementation-Version': version
        )
    }
    archiveBaseName.set('VariableManage')
}
